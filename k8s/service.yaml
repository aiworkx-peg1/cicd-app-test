apiVersion: v1
kind: Service
metadata:
  name: cicd-app-test-service
spec:
  type: NodePort
  selector:
    app: cicd-app-test
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30080
